# وضعیت پروژه Caspian.am

**آخرین به‌روزرسانی:** ۱۲ سپتامبر ۲۰۲۶  
**مخزن:** `Mosirezaei/caspian`، شاخه `main`  
**فناوری:** Next.js 15 App Router، JavaScript/JSX، Tailwind CSS، Supabase، Vercel

## وضعیت فعلی

سایت روی Vercel دیپلوی می‌شود و هر push به `main` دیپلوی خودکار ایجاد می‌کند. ساختار سایت چندزبانه است (فارسی، انگلیسی، روسی) و مسیرهای اصلی سفر، اقامت، ویزای روسیه، مقالات، رویدادها و صرافی فعال‌اند.

### بخش‌های تکمیل‌شده

- [x] ساختار سایت متمرکز بر ارمنستان و روسیه
- [x] منوی چندزبانه و آرشیو وبلاگ با فیلتر دسته‌بندی
- [x] مقالات و صفحه‌های خدمات اصلی، تورهای شهری و فستیوال‌های ارمنستان
- [x] نقشه سایت با مسیرهای قابل‌ایندکس همگام شده است
- [x] صفحه رویدادها و مسیر مستقل `/retro` (عمداً `noindex`)
- [x] فرم پرسش‌وپاسخ و CTAهای واتساپ یکپارچه شده‌اند
- [x] فوتر و منوی دسکتاپ در ۱۱ سپتامبر ۲۰۲۶ به‌روزرسانی شده‌اند
- [x] قیمت‌های ثابت باقی‌مانده از لینک‌های مرتبط ویزای روسیه حذف شده‌اند
- [x] کامپوننت بلااستفادهٔ `CurrencyRatesTable` حذف شده است

## امنیت پنل رزرو

کد کلاینت پنل رزرو دیگر هیچ رمز یا آدرس مستقیم Supabase ندارد. ورود با کوکی امن HttpOnly و درخواست‌های رزرو از API داخلی سایت عبور می‌کنند.

### اقدام ضروری قبل از استفاده از پنل

این متغیرها باید در Vercel تنظیم شوند و رمز سرویس رزرو در Supabase Edge Function نیز با رمز جدید جایگزین شود:

- `ADMIN_PASSWORD`
- `ADMIN_SESSION_SECRET`
- `RESERVATIONS_API_URL`
- `RESERVATIONS_API_PASSWORD`

بدون این پیکربندی، پنل رزرو عمداً اجازهٔ ورود یا انجام عملیات نمی‌دهد.

## قواعد ادامهٔ کار

- برای هر صفحهٔ جدید قابل‌ایندکس، `public/sitemap.xml` را به‌روزرسانی کن.
- هر صفحهٔ جدید باید عنوان، توضیح متا، canonical و محتوای سه‌زبانهٔ لازم را داشته باشد.
- مسیر `/visa/russia/multi` صفحهٔ جدا نیست؛ به `/visa/russia/business` ریدایرکت می‌شود تا لینک‌های قدیمی شکسته نشوند.
- قبل از حذف هر فایل، import و روت‌های مصرف‌کننده بررسی شود.
- رمز، توکن و کلید سرویس هرگز داخل فایل‌های کلاینت یا مستندات مخزن قرار نگیرد.

## نگهداری پیشنهادی

- بررسی دوره‌ای لینک‌های شکسته، sitemap و وضعیت ایندکس در Search Console
- به‌روزرسانی تاریخ رویدادها، فستیوال‌ها، مقررات ویزا و نرخ‌های مرجع
- پس از پیکربندی کامل پنل رزرو، یک تست ورود، نمایش، ثبت، ویرایش و حذف رکورد انجام شود
