User-agent: *
Allow: /

# Block private administration and server endpoints from crawlers
Disallow: /admin/
Disallow: /api/

# Note: /retro is no longer blocked here -- it uses a noindex meta tag
# on the page itself instead, so bots can crawl it but won't index it.

# Crawl delay for heavy bots (optional -- helps server load)
User-agent: AhrefsBot
Crawl-delay: 10

User-agent: SemrushBot
Crawl-delay: 10

User-agent: DotBot
Crawl-delay: 30

# Sitemap
Sitemap: https://caspian.am/sitemap.xml
Sitemap: https://caspian.am/sitemap-recent.xml
